#ifndef MAIN_H_
#define MAIN_H_

#include <stddef.h>
#include <limits.h>

unsigned int binary_to_uint(const char *b);
int _putchar(char c);
void print_binary(unsigned long int n);

#endif /* MAIN_H_ */
